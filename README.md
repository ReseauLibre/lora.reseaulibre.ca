This directory is a .lycheecache directory.

This README was created so this branch wouldn't be empty.

The whole thing is in git because woodpecker doesn't support caching
on its own, this should be fixed by switching to Forgejo actions, but
that doesn't support arbitrary domains for git pages.
